[coverage]
always-on = True
coverage = cu2
coverage-config = .coveragerc
coverage-report = html

[output-buffer]
always-on = True
stderr = True
stdout = True

[unittest]
code-directories = cu2
start-dir = tests
test-file-pattern = test_*.py
